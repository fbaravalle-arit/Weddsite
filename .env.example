# Public-safe configuration (exposed to the browser, prefix `NEXT_PUBLIC_`).

# Where the RSVP form posts to. Leave empty during development to no-op.
NEXT_PUBLIC_RSVP_ENDPOINT=

# Canonical site URL (used in <meta og:url> in production).
NEXT_PUBLIC_SITE_URL=https://example.com
